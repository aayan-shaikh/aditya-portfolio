---
export interface Props {
  title?: string;
  description?: string;
  image?: string;
  canonicalURL?: string;
}
import "../styles/main.scss";

const navLinks = [
  { name: "Home", href: "/" },
  { name: "About", href: "/about" },
  { name: "Portfolio", href: "/portfolio" },
  { name: "Contact", href: "/contact" },
];

const {
  title = "Aditya Mhankale | Fashion & Advertising Photographer",
  description = "Mumbai-based photographer specializing in fashion and advertising photography with expertise in retouching and post-production.",
  image = "/adi.jpg",
  canonicalURL = "https://adityamhankale.com",
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- Primary Meta Tags -->
    <title>{title}</title>
    <meta name="title" content={title} />
    <meta name="description" content={description} />
    <meta name="author" content="Aditya Mhankale" />
    <meta
      name="keywords"
      content="photography, fashion photography, advertising photography, Mumbai photographer, retouching, photo editing, portfolio, Aditya Mhankale"
    />
    <link rel="canonical" href={canonicalURL} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content="https://adityamhankale.com/wa.png" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta
      property="twitter:image"
      content="https://adityamhankale.com/wa.png"
    />
    <meta property="twitter:image:width" content="1200" />
    <meta property="twitter:image:height" content="630" />

    <!-- Favicons -->
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <link rel="shortcut icon" href="/favicon.ico" />

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Noto+Serif+Display:ital,wght@0,100..900;1,100..900&family=Reddit+Sans:ital,wght@0,200..900;1,200..900&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <nav class="navbar" id="navbar">
      <div class="container">
        <a href="/" class="navbar__logo"> Aditya Mhankale </a>

        <ul class="navbar__menu" id="navMenu">
          {
            navLinks.map((link) => (
              <li>
                <a class="navbar__link" href={link.href}>
                  {link.name}
                </a>
              </li>
            ))
          }
        </ul>

        <!-- <div class="navbar__cta">
          <a href="#" class="btn btn--enhanced btn--medium btn--shimmer"
            >Get Started</a
          >
        </div> -->

        <button
          class="navbar__hamburger"
          id="hamburger"
          aria-label="Toggle menu"
        >
          <span></span>
          <span></span>
          <span></span>
        </button>
      </div>
    </nav>

    <div id="overlay"></div>

    <main>
      <div class="madeby">
        Made with ❤️ by <a href="https://github.com/aayan-shaikh">Aayan</a>
      </div>
      <slot />
    </main>

    <footer class="footer">
      <div class="container">
        <div class="footer__grid">
          <div class="footer__column">
            <h3>About</h3>
            <p>
              Aditya mhankale is a Mumbai-based photographer specializing in
              fashion and advertising.
            </p>
            <a href="/about" class="btn btn--enhanced">Learn More</a>
          </div>

          <div class="footer__column">
            <h3>Links</h3>
            <ul>
              {
                navLinks.map((link) => (
                  <li>
                    <a href={link.href}>{link.name}</a>
                  </li>
                ))
              }
            </ul>
          </div>

          <div class="footer__column">
            <h3>Contact</h3>
            <ul>
              <li>
                <a href="mailto:adityamhankale@gmail.com"
                  >adityamhankale@gmail.com</a
                >
              </li>
              <li><a href="tel:+91 85529 33392">+91 85529 33392</a></li>
            </ul>
          </div>
        </div>
      </div>
    </footer>

    <script>
      const navbar = document.getElementById("navbar");
      const hamburger = document.getElementById("hamburger");
      const navMenu = document.getElementById("navMenu");
      const overlay = document.getElementById("overlay");

      hamburger?.addEventListener("click", () => {
        hamburger.classList.toggle("active");
        navMenu.classList.toggle("active");
        overlay.classList.toggle("active");
      });

      overlay?.addEventListener("click", () => {
        hamburger.classList.remove("active");
        navMenu.classList.remove("active");
        overlay.classList.remove("active");
      });

      window.addEventListener("scroll", () => {
        navbar.classList.toggle("scrolled", window.scrollY > 50);
      });
    </script>
  </body>
</html>
<style lang="scss">
  .madeby {
    color: white;

    a {
      text-decoration: underline;
      color: inherit;
    }

    user-select: none;
    mix-blend-mode: luminosity;

    z-index: 1;
    text-transform: capitalize;
    position: fixed;
    bottom: 10px;
    right: 30px;
    padding: 0.5rem 1rem;
    background: rgba($color: grey, $alpha: 0.1);
    backdrop-filter: blur(15px);
    border-radius: 20px;
    outline: 1px solid rgba($color: grey, $alpha: 0.2);

    @media (max-width: 1024px) {
      left: 50%;
      right: auto;
      width: max-content;
      /* Important: Override the default 'right' property */
      transform: translateX(-50%);
    }
  }
</style>
